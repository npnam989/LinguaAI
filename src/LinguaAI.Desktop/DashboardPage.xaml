<Page x:Class="LinguaAI.Desktop.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LinguaAI.Desktop"
      Title="Dashboard"
      Style="{DynamicResource PageBase}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navbar placeholder if needed -->
        
        <!-- Main Content -->
        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40" Margin="20">
            <StackPanel Width="300">
                <TextBlock Text="LinguaAI Desktop" Style="{DynamicResource HeaderTitle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Master Languages with AI" FontSize="16" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <Button Content="🎤 Pronunciation Practice" Style="{DynamicResource ModernButton}" Click="PracticeButton_Click" />
                <Button Content="📚 Vocabulary Builder" Style="{DynamicResource ModernButton}" Click="VocabularyButton_Click" />
                <Button Content="💬 Conversation Practice" Style="{DynamicResource ModernButton}" Click="ConversationButton_Click" />
                <Button Content="📖 Reading Comprehension" Style="{DynamicResource ModernButton}" Click="ReadingButton_Click" />
                <Button Content="✍️ Writing Correction" Style="{DynamicResource ModernButton}" Click="WritingButton_Click" />

                <ToggleButton x:Name="ThemeToggle" Content="🌙 Dark Mode" Margin="0,20,0,0" HorizontalAlignment="Center" Click="ThemeToggle_Click" Padding="10,5" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
