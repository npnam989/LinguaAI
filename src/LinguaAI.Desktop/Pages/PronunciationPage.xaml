<Page x:Class="LinguaAI.Desktop.Pages.PronunciationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      Title="PronunciationPage"
      Style="{DynamicResource PageBase}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back Button & Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="⬅" Padding="10,5" Click="BackButton_Click" Style="{DynamicResource ModernButton}" Width="40"/>
            <TextBlock Text="Pronunciation Practice" Style="{DynamicResource HeaderTitle}" Margin="20,0,0,20" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Target Phrase Input -->
        <StackPanel Grid.Row="1" Margin="0,20">
            <TextBlock Text="Phrase to Practice:" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TargetInput" Text="Hello world" FontSize="18" Padding="10"/>
        </StackPanel>

        <!-- Visualizer & Controls -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20">
            <Canvas x:Name="AudioCanvas" Width="400" Height="100" Background="#f3f4f6"/>
            <Button x:Name="RecordButton" Content="🎤 Record" 
                    Width="120" Height="50" FontSize="18" Margin="0,20,0,0"
                    Background="#2563eb" Foreground="White"
                    Click="RecordButton_Click"/>
        </StackPanel>

        <!-- Status -->
        <TextBlock x:Name="StatusText" Grid.Row="3" Text="Ready" HorizontalAlignment="Center" Foreground="#6b7280"/>

        <!-- Results -->
        <ScrollViewer Grid.Row="4" Margin="0,20,0,0">
            <StackPanel>
                <TextBlock Text="Result:" FontWeight="Bold"/>
                <TextBlock x:Name="ResultScore" FontSize="24" Foreground="#2563eb" Margin="0,5"/>
                <TextBlock x:Name="ResultDetails" TextWrapping="Wrap" FontSize="16"/>
                <TextBlock x:Name="ResultFeedback" TextWrapping="Wrap" Foreground="#4b5563" Margin="0,10"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
